# Mid-Course Project Prompt Log

This log summarizes the main AI-assisted work. AI-generated suggestions and changes were treated as drafts: I reviewed the relevant code, behavior, and tests before accepting them. When a suggested or generated command failed, I reported the failure rather than presenting it as successful.

| Work item | Objective and prompt summary | Files provided to the AI | AI output or recommendation | Engineering review and decision |
| --- | --- | --- | --- | --- |
| Feature 1 backend due dates | Add an optional ISO-format `due_date` to create, update, and task responses without disturbing existing fields. | `app/main.py`, `app/models.py`, `app/storage.py` | Suggested model and endpoint changes for optional date creation, updates, and clearing. | Reviewed Pydantic validation, patch semantics, and existing API behavior. Accepted after revision and backend verification. |
| Feature 1 tests | Add focused coverage for valid, invalid, updated, cleared, and overdue due dates. | Due-date backend files and `tests/test_tasks.py` | Proposed cases for date validation and overdue boundary behavior. | Checked that tests exercised public API behavior, including HTTP 422, due today, and `Done` tasks. Accepted after review. |
| Feature 1 frontend due-date support | Add due-date inputs to create/edit flows and display dates on cards. | `frontend/index.html` and the backend response shape | Suggested form, payload, edit-prefill, clearing, and rendering changes. | Manually tested creation, editing, clearing, and display in the browser. Accepted after adjustment to the existing modal/rendering flow. |
| Overdue indicators | Visibly identify unfinished tasks whose due date is earlier than today. | Backend overdue rules, tests, and `frontend/index.html` | Recommended keeping the overdue rule consistent between backend filtering and frontend card rendering. | Verified the strict date boundary and exclusion of `Done` tasks in tests and the browser. Accepted; no frontend overdue filter was claimed or added. |
| Deletion feature | Preserve existing deletion behavior while adding the selected features. | `app/main.py`, `app/storage.py`, `frontend/index.html`, and relevant tests | Recommended retaining the existing API-driven delete and refresh path. | Reviewed deletion behavior and confirmed it remained integrated with board refreshes. Accepted as preserved behavior, not redesigned. |
| Feature 2 backend filters | Extend `GET /tasks` with `search`, `status`, `priority`, `assignee`, and `overdue`, using server-side AND composition. | `app/main.py`, `app/storage.py`, `app/models.py` | Suggested normalized, case-insensitive substring matching, ignored blank text filters, and sequential AND filtering. | Reviewed validation, unassigned-task behavior, existing filters, and the decision not to add backend sorting. Accepted after revision. |
| Feature 2 backend tests | Cover text matching, assignee behavior, blank/invalid inputs, and combined filters. | Backend filter implementation and `tests/test_tasks.py` | Proposed focused positive, negative, validation, and composition cases. | Reviewed assertions against endpoint behavior and ran the full repository suite. Accepted when the final result was `37 passed in 0.80s`. |
| Feature 2 frontend filters | Add filter controls and server-built query strings while preserving the Kanban architecture. | `frontend/index.html` and `GET /tasks` query behavior | Recommended `URLSearchParams`, omission of empty values, Apply/Clear actions, and reusing active filters during refreshes. | Manually checked individual and combined filters, clearing, and persistence after create/edit/delete/drag-and-drop. Accepted after review. |
| Break Test guidance | Mutate one important condition per feature to demonstrate that tests detect real defects. | Overdue and search implementations plus `tests/test_tasks.py` | Suggested changing `<` to `<=` for overdue and substring to exact title matching for search, then restoring each mutation. | Confirmed the failures matched the introduced defects. The Feature 2 broken run was `2 failed, 35 passed in 1.21s`; after restoration it was `37 passed in 0.80s`. Accepted as verification evidence only. |
| Final documentation | Align stories, decisions, verification evidence, prompt history, reflection, and setup guidance with the completed implementation. | `docs/midcourse/*.md`, `README.md`, and the supplied completion evidence | Recommended concise, evidence-based updates and removal of claims about a frontend overdue filter. | Cross-checked every claim against the implementation summary and recorded results. Revised the drafts to avoid invented dates, screenshots, outputs, and Node.js success claims. |
